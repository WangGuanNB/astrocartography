# 首页曝光下跌排查与低风险修复计划

更新时间：2026-09-21

## 目标

在尽量不影响首页现有 SEO 排名、搜索意图和付费转化路径的前提下，排查首页曝光下跌原因，并采用单变量、可回滚的方式逐步优化。

原则：先修复技术新鲜度问题，再观察完整周期；不在同一次发布中同时修改标题、H1、价格、CTA 和页面结构。

## 当前数据结论

数据来源：

- GSC 属性：`sc-domain:astrocarto.org`
- 报告：[gsc-report-2026-09-21.xlsx](</Users/wangguan/Desktop/Banner/Code/my-page-analytics-factory/DB/astrocartography/output/reports/gsc-report-2026-09-21.xlsx>)
- 数据区间：2026-06-24 至 2026-09-21

最近 7 天对比前 7 天：

- 全站展示下降 12.9%，点击下降 30.4%；平均排名约为 8.49，基本稳定。
- 英文首页展示下降 34.3%，点击下降 40.5%。
- 首页贡献了全站约 65% 的点击损失和约 76% 的展示损失。
- 首页点击损失主要来自展示减少，而不是 CTR 大幅下降。
- Plus 在 2026-09-18 开放，但下降从 2026-09-15 已经开始，目前没有证据证明 Plus 发布是 SEO 下跌原因。
- 2026-09-21 是未完成日期，不能把当天低数据当作算法惩罚或崩溃。

页面级对比中，意大利、德语、西语首页也有下降，葡萄牙语首页反而上涨。因此不能把原因简单归结为首页某一个按钮或单一文案。

## 已执行的低风险技术修复

### 1. 首页 ISR 缓存从 7 天改为 1 天

文件：`src/app/[locale]/(default)/page.tsx`

变更：

```ts
export const revalidate = 86400;
```

影响：

- 首页内容最长约 1 天重新验证一次，不再长期保留 7 天旧版本。
- 不改变 URL、标题、H1、canonical、hreflang、布局或用户流程。
- 不会直接提升排名，只是减少页面内容更新后的滞后时间。
- 会增加少量重新生成请求，属于可接受的低风险成本。

### 2. 修正首页及语言首页 sitemap 的 lastmod

文件：`public/sitemap.xml`

首页及语言首页的 `lastmod` 从错误的 2026-05-18 更新为实际最近一次首页内容变更日期 2026-09-19。

注意：后续只有页面内容真正发生变化时才更新 `lastmod`，不要每次部署都伪造为当天日期。

## 部署后的验收清单

部署后检查：

1. 首页返回 HTTP 200。
2. 首页仍然是 `index, follow`。
3. canonical 仍指向对应语言首页。
4. hreflang 没有丢失或指向错误 URL。
5. `/sitemap.xml` 可以正常访问。
6. 首页及语言首页 URL 正确，`lastmod` 为真实日期。
7. 页面没有 404、500、空白或明显的移动端布局异常。
8. 首页标题、H1、套餐、Ask Me 和页面结构未发生意外变化。

构建验收结果：

- `pnpm build` 通过；
- Next.js 构建输出显示首页及各语言首页 `Revalidate: 1d`；
- 构建过程中出现的 `src/db/index.ts` 动态依赖警告为项目原有警告，本次修改没有新增编译错误。

## 观察周期

至少观察两个完整的 7 天周期，不能使用当前未完成日期做结论。

每周记录：

- 首页展示、点击、CTR、平均排名；
- `astrocartography`；
- `astrocartography chart`；
- `free astrocartography chart`；
- `astrocartography calculator`；
- 移动端与桌面端；
- 英文、意大利语、德语、西语、葡萄牙语首页。

## 后续决策规则

### 展示恢复

保持当前版本，不继续改首页 SEO，避免引入新的变量。

### 展示不恢复但排名稳定

优先增加首页的信息承接和内部链接，不删除已有内容。增加：

- How to use the free astrocartography chart；
- Check a City；
- Compare Cities；
- Save Your Research；
- Ask Me；
- FAQ。

### 核心词排名持续下降超过 1 个位置

只先测试 `<title>`，不同时修改 H1。候选标题：

```text
Free Astrocartography Chart & Map Calculator | AstroCarto
```

测试至少 14 天，再决定是否修改 H1。

### 只有移动端下降

先检查移动端加载、地图阻塞、布局跳动和 Core Web Vitals，不先改标题。

### 只有某个语言版本下降

只优化对应语言首页，不修改英文首页和全站模板。

## 暂不执行的动作

- 不回滚 Plus。
- 不修改套餐价格。
- 不改变 Ask Me 的主转化路径。
- 不删除首页已有内容。
- 不修改 URL、canonical、hreflang。
- 不同时改标题、H1、文案、价格和页面布局。

## 版本记录

### 2026-09-21

- 完成 GSC 首页和页面级数据排查。
- 确认首页是主要点击损失来源，但尚未证明存在单一算法惩罚。
- 完成首页 ISR 7 天改 1 天。
- 修正首页及语言首页 sitemap 的过期 `lastmod`。
- `pnpm build` 构建通过，确认首页重新验证周期为 1 天。
- 暂不修改首页标题、H1、套餐和 CTA。

### 下一次更新

部署后补充：部署时间、验收结果、7 天和 14 天 GSC 对比数据，以及是否进入标题测试阶段。
